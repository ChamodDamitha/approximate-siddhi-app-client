1. Clone the project and enter "mvn clean install" in a terminal
2. Copy "approximateClient-1.0-SNAPSHOT.jar" inside the target directory and "run.sh" to another directory.
3. Enter "sudo chmod a+x run.sh" in a terminal
4. Run the "run.sh"  
